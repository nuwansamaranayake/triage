"""CareerCompiler engine: the honest analyzer.

The LLM senses (fact extraction, JD parsing — both span/schema-constrained); deterministic
code decides (matching, fit verdict). Phase 1 ships no generation: the fact graph, the typed
requirements, and the Fit Report are the product.
"""
